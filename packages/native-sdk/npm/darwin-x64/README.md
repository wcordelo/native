# @native-sdk/cli-darwin-x64

The prebuilt `native` binary for macOS on Intel (x64). Installed automatically as an optional dependency of [`@native-sdk/cli`](https://www.npmjs.com/package/@native-sdk/cli) — install that package instead of this one.
