# @native-sdk/cli-linux-x64-musl

The prebuilt `native` binary for Linux x64 (musl). Installed automatically as an optional dependency of [`@native-sdk/cli`](https://www.npmjs.com/package/@native-sdk/cli) — install that package instead of this one.
