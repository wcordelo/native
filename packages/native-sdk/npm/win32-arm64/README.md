# @native-sdk/cli-win32-arm64

The prebuilt `native` binary for Windows on ARM (arm64). Installed automatically as an optional dependency of [`@native-sdk/cli`](https://www.npmjs.com/package/@native-sdk/cli) — install that package instead of this one.
