# @native-sdk/cli-linux-arm64-gnu

The prebuilt `native` binary for Linux arm64 (glibc). Installed automatically as an optional dependency of [`@native-sdk/cli`](https://www.npmjs.com/package/@native-sdk/cli) — install that package instead of this one.
