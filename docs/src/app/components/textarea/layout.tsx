import { pageMetadata } from "@/lib/page-metadata";

export const metadata = pageMetadata("components/textarea");

export default function TextareaLayout({ children }: { children: React.ReactNode }) {
  return children;
}
